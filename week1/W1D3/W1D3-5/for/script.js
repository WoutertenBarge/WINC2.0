/* **For loop:**

- Gebruik dezelfde array als startpunt.
- Schrijf een for loop die de kleuren in de console logged en stopt 
wanneer alle kleuren geprint zijn.
- Gebruik de lengte van de array in je for loop: `array.length`

Het schrijven van een for of while loop kan best veel ruimte in beslag nemen. 
Daarnaast is het ook makkelijk om een foutje te maken. */



/* while loop

const colors = [

    'yellow',
    'blue',
    'red',
    'orange'
];

let index = 0;

while ( index < colors.lenght) {

    console.log(colors[index]);
    index++;
}; */

//for loop

/* const colors = [ 
    'yellow',
     'blue',
    'red',
    'orange'
];

    

for (let i = 0; i < colors.lenght; i++ ) {

        console.log(colors[i]);
    } */







    const colors = ['yellow', 'blue', 'red', 'orange'];

    for (let i = 0; i < colors.length; i++) {
        console.log(colors[i]);
    }